language: android
android:
  components:
    - tools
    - platform-tools
    - android-22
    - tools
    - build-tools-22.0.0

jdk: oraclejdk8
before_install:
  - chmod +x gradlew

# Extend compile timeout to 30 minutes; gwt easily takes 10+.
script: travis_wait 30 ./gradlew clean build